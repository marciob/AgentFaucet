# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Relayer wallet (server-side only — sends gasless claim txs)
PRIVATE_KEY=0x...

# Agent JWT signing secret (falls back to SUPABASE_SERVICE_ROLE_KEY if unset)
AGENT_JWT_SECRET=

# Contract address for sponsor page
NEXT_PUBLIC_FAUCET_POOL_ADDRESS=0xA71D77C1451d169A798c8260582B7740704B843A

# Optional — defaults to https://agentfaucet.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000
